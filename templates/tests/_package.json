{
    "devDependencies": {
        "@vitest/ui": "^0",
        "vitest": "^0"
    },
    "scripts": {
        "test": "vitest run",
        "test:ui": "vitest --ui",
        "test:watch": "vitest"
    }
}
